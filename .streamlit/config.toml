[theme]
primaryColor = "#1B2D5A"          # navy — used for accents (active tab underline, links)
backgroundColor = "#FFFFFF"        # main page background
secondaryBackgroundColor = "#F0F2F6"  # sidebar / widget background
textColor = "#1B2D5A"              # navy text
font = "sans serif"